import React from 'react'

import { Button } from '../../components/Button';

export default {
    title: 'Components/Button',
    components: Button
};

export const Default = () => <Button > inserir </Button >